# Pomodoro
Basic Pomodoro timer built using Qt6 framework 
## Build
Install qt6 if you haven't already
```bash
sudo dnf install qt6base qt6base-devel
```
Build the project
```bash
mkdir build && cd build
cmake ..
make
```